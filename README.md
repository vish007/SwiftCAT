# SwiftCAT M6 Prototype

Implements deterministic agentic-AI endpoints with guardrails, audited action rationale, and a simple UI with a persistent AI widget and insights panels.

## Run

```bash
pip install -r requirements.txt
python app.py
```

## Test

```bash
pytest
```
